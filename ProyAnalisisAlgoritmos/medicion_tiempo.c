#include <stdio.h>  
#include <stdlib.h>

int main(){
    int n;
    do{
        printf("Ingrese un numero entero positivo:\n");
        scanf("%d",&N);
    }while(N<0);
    
    for(int contador=0;contador++; contador<=N){
        contador=contador+1;
    }

    system("pause");    
    return 0;
}